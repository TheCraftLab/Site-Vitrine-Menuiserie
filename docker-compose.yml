services:
  vitrine:
    build: .
    container_name: vitrine-menuiserie
    ports:
      - "${PUBLIC_PORT:-8081}:3000"
    environment:
      ADMIN_PATH: ${ADMIN_PATH:-atelier-admin}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-flake}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-Acfn3puf$lcMdvc}
      TZ: ${TZ:-Europe/Paris}
    volumes:
      - vitrine_data:/data
    restart: unless-stopped

volumes:
  vitrine_data:
